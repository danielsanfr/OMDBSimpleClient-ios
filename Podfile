# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OMDBSimpleClient' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OMDBSimpleClient
  pod 'Alamofire', '4.5.1'
  pod 'RealmSwift', '3.0.0'
  pod 'RxAlamofire', '4.0.0'
  pod 'RxRealm', '0.7.3'

  target 'OMDBSimpleClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OMDBSimpleClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
